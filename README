Mini Distribution "opt" build files
-----------------------------------

This collection of build files include optional features. Build files in this
repository may only depend on buildfiles in core and opt.
